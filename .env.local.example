# Supabase — públicas (expuestas al browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — solo servidor (nunca exponer al cliente)
SUPABASE_SERVICE_ROLE_KEY=

# LiteLLM — solo servidor
LITELLM_BASE_URL=
LITELLM_API_KEY=

# Redis — solo servidor
REDIS_URL=

# OpenAI — solo servidor (embeddings para RAG)
OPENAI_API_KEY=

# Providers - solo servidor (usadas por LiteLLM)
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Cron — solo servidor (Vercel Cron Jobs)
CRON_SECRET=

